# LEGACY: pip requirements used by the Docker-based msg_relay runtime.
# Dependencies are now declared in host/main/pyproject.toml (openrdk package).
pyserial==3.5
pyudev==0.24.1
fastapi==0.115.6
uvicorn==0.32.1
websockets==13.1
